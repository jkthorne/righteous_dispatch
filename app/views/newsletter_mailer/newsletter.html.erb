<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
      line-height: 1.6;
      color: #1f2937;
      background-color: #f3f4f6;
      margin: 0;
      padding: 0;
    }
    .wrapper {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #1f2937;
      color: #ffffff;
      padding: 20px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 14px;
      letter-spacing: 2px;
      font-weight: bold;
    }
    .content {
      background-color: #ffffff;
      padding: 30px;
      border: 1px solid #e5e7eb;
    }
    .content h1, .content h2, .content h3 {
      color: #1f2937;
      margin-top: 0;
    }
    .content a {
      color: #3b82f6;
    }
    .content blockquote {
      border-left: 3px solid #3b82f6;
      margin: 20px 0;
      padding-left: 20px;
      color: #4b5563;
      font-style: italic;
    }
    .content ul, .content ol {
      padding-left: 20px;
    }
    .content li {
      margin-bottom: 8px;
    }
    .footer {
      padding: 20px;
      text-align: center;
      font-size: 12px;
      color: #6b7280;
    }
    .footer a {
      color: #6b7280;
      text-decoration: underline;
    }
    .view-browser {
      text-align: center;
      padding: 10px;
      font-size: 11px;
      color: #9ca3af;
    }
    .view-browser a {
      color: #9ca3af;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="view-browser">
      <a href="<%= @view_in_browser_url %>">View this email in your browser</a>
    </div>

    <div class="header">
      <h1>RIGHTEOUSDISPATCH</h1>
    </div>

    <div class="content">
      <% if @newsletter.preview_text.present? %>
        <p style="color: #6b7280; font-size: 14px; margin-bottom: 20px;"><%= @newsletter.preview_text %></p>
      <% end %>

      <%= @newsletter.content %>
    </div>

    <div class="footer">
      <p>
        Sent by <%= @user.name %> via RighteousDispatch
      </p>
      <p>
        <a href="<%= @unsubscribe_url %>">Unsubscribe</a>
      </p>
    </div>
  </div>
</body>
</html>
