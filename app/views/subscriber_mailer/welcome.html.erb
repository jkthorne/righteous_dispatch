<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
      line-height: 1.6;
      color: #1f2937;
      background-color: #f3f4f6;
      margin: 0;
      padding: 0;
    }
    .wrapper {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background-color: #1f2937;
      color: #ffffff;
      padding: 20px;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 14px;
      letter-spacing: 2px;
      font-weight: bold;
    }
    .content {
      background-color: #ffffff;
      padding: 30px;
      border: 1px solid #e5e7eb;
    }
    .content h2 {
      color: #1f2937;
      margin-top: 0;
      font-size: 20px;
    }
    .content p {
      margin: 0 0 16px 0;
    }
    .footer {
      padding: 20px;
      text-align: center;
      font-size: 12px;
      color: #6b7280;
    }
    .footer a {
      color: #6b7280;
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="header">
      <h1>RIGHTEOUSDISPATCH</h1>
    </div>

    <div class="content">
      <h2><%= @user.welcome_email_subject %></h2>

      <% if @subscriber.name.present? %>
        <p>Hi <%= @subscriber.name %>,</p>
      <% end %>

      <p><%= simple_format(@user.welcome_email_content) %></p>

      <p>
        <em>— <%= @user.name %></em>
      </p>
    </div>

    <div class="footer">
      <p>
        Sent by <%= @user.name %> via RighteousDispatch
      </p>
      <p>
        <a href="<%= @unsubscribe_url %>">Unsubscribe</a>
      </p>
    </div>
  </div>
</body>
</html>
